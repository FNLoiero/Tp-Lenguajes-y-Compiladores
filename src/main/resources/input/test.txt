init {
    a, b, d, z : Float
    c1, c2, c3, x, c , contador: Int
    b, base, y : String
}

a := 99999.99
a := 99.
a := .9999
b := "@sdADaSjfla%dfg"
b := "asldk  fh sjf"
c1 := 10
c2 := 456
c3 := 500
c3 := -500
c3 := -500.
c3 := -500.001

x := 27 - c1
x := c1 + 500
x := c1 * c3
d := a / z
c := d * (e - 21) / 4

#+ “ewr” es una cte string +#
write("ewr")
write(b)
read(base)

#+ “ewr” es una cte string +#
if (c1 > c2)
{
    write("c1 es mas grande que c2")
}

if (c1 > c2)
{
    write("c1 es mas grande que c2")
}
else
{
    write("c1 es mas chico o igual a c2")
}

#+ “ewr” es una cte string +#

if (c1 > c2 AND c3 > c2)
{
    #+ “ewr” es una cte string +#
    write("c1 > c2 AND c3 > c2")
}

if (c1 > c2 OR c3 > c2)
{
    #+ “ewr” es una cte string +#
    write("c1 > c2 o c3 >c2")
}

if (NOT c1 > c2)
{
    write("c1 no es mas grande que c2")
}

if (c1 > c2 AND c3 > c2)
{
    write("c1 > c2 AND c3 > c2")
}
else
{
    write("c1 <= c2")
    write("o c3 es mas chico a c2")
}

c1 := 1 #+ “ewr” es una cte string +#
c2 := 3 #+ “ewr” es una cte string +#

#+ “ewr” es una cte string +#

if(c1 < c2){
	write("Todavia no se anida")
}
else if(c1 > c2){
	write("Apa, se esta anidando")
}
else{
	write("Funciona el anidamiento")
}

while (c1 > c2)
{
    write("c1 es mas grande que c2")
    c1 := c1 + 1
}

while (isZero( contador - 10 ))
{
    write("El contador vale 10")
    contador := contador - 1
}
if (isZero( s*7+1-h/2 ))
{
    write("La expresion es igual a cero")
}


y := equalExpressions(a + b, 5, b * 2, 3 + 2, a + b)  
y := equalExpressions(0, -5, a + b)  

#+ A continuacion la seccion con lineas que generan error de compilacion : +#
#+ 1. Error de string, el literal no está cerrado correctamente. +#
#+ b := "cadena sin cerrar +#

#+ 2. Error, operación incompleta (falta operando derecho). +#
#+ a := 10 - +#

#+ 3. Error de sintaxis, falta cerrar el paréntesis. +#
#+ if (a < b +#

#+ 4. Error léxico, formato incorrecto para un flotante. +#
#+ c := 10.2.4 +#

#+ 5. Error de sintaxis, símbolo de asignación incorrecto +#
#+ c = 5 +#

#+ 6. Error de condición incompleta (falta el operando derecho). +#
#+ if (a > ) +#
#+ { +#
#+    write("a es mas grande que b") +#
#+ } +#

#+ 7. Error, "write" solo permite cadenas o variables, no expresiones. +#
#+ write(a + b) +#

#+ 8. Error, falta el bloque {} para la sentencia del if. +#
#+ if (a > b) write("Sin bloque") +#

#+ 9. Error, if con doble condición +#
#+ if (a > b > c) {write("Doble condición")} +#

#+ 10. Constante entera fuera de rango +#
#+ c := "Esto es un string que tiene que superar los cincuenta caracteres de largo." +#


#+ 11: Constante cadena excede los 30 caracteres: c1 es mas grande que c2 y c3 es mas grande que c2 +#
#+ write("c1 es mas grande que c2 y c3 es mas grande que c2") +#